## Repo Rules

- After completing each user request that changes files, ask whether to create a git commit.
- If a commit is requested, use a message that summarizes all changes since the last commit.
